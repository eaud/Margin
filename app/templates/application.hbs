<body class="mirage stellar">
  <div class="doc_header">
    <div class="mainmenu-block">
      <nav class="menu-block">
        <ul class="nav">
          {{#if session.isAuthenticated}}
          <div class="menu-block-label" {{action 'logout'}}>Logout</div>
          {{else}}

          <!-- <button {{action 'openModal' 'users/new'}}>Open modal</button> -->
            <div class="menu-block-label">{{#link-to 'users.new'}}Sign Up{{/link-to}}</div>
            <div class="menu-block-label">{{#link-to 'login'}}Login{{/link-to}}</div>
          <div class="menu-block-label">
              <div class="row">
                {{outlet 'login'}}
              </div>
          </div>
          {{/if}}
        </ul>
        <hr>
        <ul class="nav">
          {{#link-to 'notebooks'}}
            <div class="menu-block-label">
              Show Notebooks
            </div>
          {{/link-to}}
          {{outlet 'sidebar'}}
        </ul>
      </nav>
    </div>
  </div>
      {{#if session.isAuthenticated}}
        <div class="main-wrapper">
          <div class="view-wrapper">
            <div class="topbar-wrapper">
              <div class="topbar-wrapper-mobile-nav">
                <a href="#" class="topbar-togger js-minibar-toggler">
                  <i class="icon ion-ios-arrow-back hidden-md-down"></i>
                  <i class="icon hidden-lg-up ion-navicon-round"></i>
                </a>
                <a href="#" class="topbar-togger pull-xs-right hidden-lg-up js-nav-toggler">
                  <i class ="icon ion-android-person"></i>
                </a>
                <h1 id="title" class="topbar-wrapper-logo demo-logo">{{link-to "MARGIN" 'application'}}</h1>
              </div>
            </div>

            {{outlet 'editor'}}

          </div>
        </div>
      {{else}}
      {{/if}}
</body>
